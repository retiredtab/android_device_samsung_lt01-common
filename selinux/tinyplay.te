# tinyplay
type tinyplay, domain;
type tinyplay_exec, exec_type, file_type;

init_daemon_domain(tinyplay)

allow tinyplay audio_device:chr_file { rw_file_perms };
allow tinyplay audio_device:dir { r_dir_perms };
